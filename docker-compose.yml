version: '3'

services:
  postgres:
    image: postgres
    container_name: "pandoradb"
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: postgres